services:
  naver-ads-api:
    build: .
    container_name: naver-ads-api
    restart: unless-stopped
    ports:
      - "127.0.0.1:8400:8400"
    env_file:
      - .env
    environment:
      - MCP_WRITE_ENABLED=true
    networks:
      - n8n-network

networks:
  n8n-network:
    external: true
