# 얼마에요 백업 전체 대조 감사 (2026-03-10)

## 기준

- 백업 원본 폴더: `/Users/jangjiho/Downloads/얼마에요 백업파일`
- 비교 대상: 운영 CRM `crm-proxy`
- 재검증 시각: `2026-03-10 16:xx KST`
- 주의: 2026년 이후 CRM에서 직접 작성한 명세표 데이터는 보존 대상이며, 본 감사는 레거시 백업 적재 상태만 검증한다.

## 최종 판정

- 레거시 거래내역 `97,086건`은 CRM `txHistory`에도 `97,086건`으로 모두 존재한다.
- 백업 품목 `3,008건` 중 누락으로 보였던 `10226 / 압화건조시트6p/수입`도 현재 CRM에 존재한다.
- `거래처명 공란 + 장부명만 존재`하던 누락 고객 `15건`은 현재 legacy_id 기준으로 모두 CRM 고객에 연결되어 있다.
- 해당 고객들의 분리 거래 `237건`도 현재 `customer_name`이 채워져 고객 조회/집계에서 분리되지 않는다.
- 현재 P1에서 실제로 남아 있는 불일치는 `outstanding_balance 6건`뿐이다.

## 현재 남은 핵심 이슈

### 1. 레거시 잔액 불일치 6건

- 아래 6건은 백업 거래처 잔액과 CRM `outstanding_balance`가 아직 다르다.
- 공통점은 단순 누락보다 `레거시 잔액 baseline`과 `복구 이후 CRM 운영 잔액` 사이 정책 차이에 가깝다.

| legacy_id | 고객명 | 레거시 잔액 | 현재 CRM 잔액 | 차이 |
|-----------|--------|-------------|---------------|------|
| 114 | 신재승 회장님 | -1,207,900 | -581,000 | +626,900 |
| 170 | 채명례 이사장님 | -774,130 | -703,930 | +70,200 |
| 7791 | 아름화원(김미자 님) | 0 | 79,700 | +79,700 |
| 8008 | 서은희 회장님 | -163,000 | 37,200 | +200,200 |
| 30160 | 드라이데코 | 0 | 72,500 | +72,500 |
| 30653 | 대아수목원 | 0 | 1,750,000 | +1,750,000 |

### 2. 잔액 정의 문서화 필요

- 지금 남은 문제는 "백업값을 그대로 덮어쓸지", "레거시 baseline + CRM 이후 변동분을 유지할지" 정책이 확정되지 않았다는 점이다.
- 따라서 이 6건은 이관 실패보다는 회계 기준 정의 미확정 이슈로 분류하는 편이 정확하다.

## 이번 재감사에서 해소된 항목

### 1. 고객 마스터 누락 15건 해소

- 과거 문서에서는 `거래처명 공란 고객 15건`이 통째로 누락된 것으로 기록돼 있었다.
- 현재 재검증 결과 legacy_id 기준 미생성 고객은 `0건`이다.

### 2. 분리 거래 237건 해소

- 과거에는 누락 고객 관련 거래 `237건`이 `txHistory.customer_name` 공란으로 남아 조회/집계에서 분리된 상태였다.
- 현재 재검증 결과 해당 조건의 미연결 거래는 `0건`이다.

### 3. 누락 품목 1건 해소

- 과거에 누락으로 표기했던 `10226 / 압화건조시트6p/수입`은 현재 CRM 제품 마스터에 존재한다.

## 운영 복구 상태

- 운영 DB 안전 백업:
  - `~/nocodb/nocodb_data/noco.db.pre-recovery-20260310-134105`
  - 기존 백업 `~/nocodb/nocodb_data/noco.db.bak_20260305`
- CRM 명세표 복구:
  - `tbl_Invoices` `16건`
  - `tbl_InvoiceItems` `127건`
  - 날짜 범위 `2026-02-11 ~ 2026-03-10`
  - 복구 근거: `nc_audit_v2`의 `DATA_DELETE` / `DATA_BULK_DELETE`
- 레거시 거래내역 정규화:
  - `txHistory.customer_name` 공란 중 고객 매핑 가능 `237건` 보정 완료
  - 현재 재검증 결과 `remaining_blank_tx_for_blank-name-customers = 0`
- 운영 화면 재검증:
  - `거래/명세표 조회`에 레거시/CRM 이력이 함께 노출
  - `명세표 작성`에 최신 CRM 명세표 노출
  - `미수금`과 고객 조회에서 공란 고객 계열이 분리되지 않음

## 재검증 방법

- `python3 scripts/repair_legacy_backup.py`
  - 최신 dry-run 기준: `customer_updates 6`, `customer_creates 0`, `tx_updates 0`, `product_creates 0`
- 추가 검증 스크립트로 아래를 재확인함:
  - `blank_named_customers_missing = 0`
  - `blank_named_tx_missing_customer_name = 0`
  - `balance_mismatches = 6`
  - `missing_products = 0`

## 원인 정리

- 고객 누락, 분리 거래, 누락 품목 문제는 이미 복구가 완료됐다.
- 남은 항목은 데이터 적재 실패보다 `잔액 계산 기준`과 `운영 후속 거래 반영 방식`을 명시하지 않은 데서 발생한다.

## 다음 액션

1. 잔액 불일치 6건에 대해 `레거시 원장`, `복구 후 CRM 명세표`, `입금 반영 이력`을 나란히 대조한다.
2. `outstanding_balance`를 어떤 기준일/공식으로 유지할지 문서화한다.
3. 정책 확정 후 6건을 수동 보정하거나, 기준식을 코드로 재계산한다.
4. 이후 P2로 `고객리스트 원본 충돌/오염 필드 표시 정책`을 UX 차원에서 정리한다.
