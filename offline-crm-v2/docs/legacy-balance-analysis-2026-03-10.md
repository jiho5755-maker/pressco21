# 레거시 잔액 불일치 6건 원인 분석 (2026-03-10)

## 결론

- 현재 남아 있는 `outstanding_balance` 불일치 6건은 이관 누락이 아니라 `잔액 기준 정의` 문제다.
- 실제 운영 데이터는 아래 공식으로 설명된다.

`현재 CRM 고객 잔액 = 레거시 tradebook.balance + 미수 CRM 명세표 합계`

- 따라서 감사 문서에서 보이던 `백업값과 CRM값 불일치`는 오류가 아니라, `레거시 잔액 baseline` 위에 2026년 CRM 운영분이 더해진 결과로 봐야 한다.

## 대조 결과

| legacy_id | 고객명 | 레거시 잔액 | CRM 미수 명세표 합계 | 현재 CRM 고객 잔액 | 판정 |
|-----------|--------|-------------|----------------------|-------------------|------|
| 114 | 신재승 회장님 | -1,207,900 | 626,900 | -581,000 | baseline + CRM 미수 일치 |
| 170 | 채명례 이사장님 | -774,130 | 70,200 | -703,930 | baseline + CRM 미수 일치 |
| 7791 | 아름화원(김미자 님) | 0 | 79,700 | 79,700 | baseline + CRM 미수 일치 |
| 8008 | 서은희 회장님 | -163,000 | 200,200 | 37,200 | baseline + CRM 미수 일치 |
| 30160 | 드라이데코 | 0 | 72,500 | 72,500 | baseline + CRM 미수 일치 |
| 30653 | 대아수목원 | 0 | 1,750,000 | 1,750,000 | baseline + CRM 미수 일치 |

## 고객별 근거

### 1. 신재승 회장님 (`legacy_id=114`)

- 레거시 잔액: `-1,207,900`
- CRM 미수 명세표:
  - `INV-20260309-144554` (`2026-03-09`) `626,900원`, 미수
- 계산:
  - `-1,207,900 + 626,900 = -581,000`
- 현재 고객 `outstanding_balance`: `-581,000`

### 2. 채명례 이사장님 (`legacy_id=170`)

- 레거시 잔액: `-774,130`
- CRM 미수 명세표:
  - `INV-20260310-094003` (`2026-02-11`) `70,200원`, 미수
- 계산:
  - `-774,130 + 70,200 = -703,930`
- 현재 고객 `outstanding_balance`: `-703,930`

### 3. 아름화원(김미자 님) (`legacy_id=7791`)

- 레거시 잔액: `0`
- CRM 미수 명세표:
  - `INV-20260309-144433` (`2026-03-09`) `79,700원`, 미수
- 계산:
  - `0 + 79,700 = 79,700`
- 현재 고객 `outstanding_balance`: `79,700`

### 4. 서은희 회장님 (`legacy_id=8008`)

- 레거시 잔액: `-163,000`
- CRM 미수 명세표:
  - `INV-20260309-173326` (`2026-03-09`) `200,200원`, 미수
- 계산:
  - `-163,000 + 200,200 = 37,200`
- 현재 고객 `outstanding_balance`: `37,200`

### 5. 드라이데코 (`legacy_id=30160`)

- 레거시 잔액: `0`
- CRM 미수 명세표:
  - `INV-20260309-103734` (`2026-03-09`) `72,500원`, 미수
- 계산:
  - `0 + 72,500 = 72,500`
- 현재 고객 `outstanding_balance`: `72,500`

### 6. 대아수목원 (`legacy_id=30653`)

- 레거시 잔액: `0`
- CRM 미수 명세표:
  - `INV-20260310-090858` (`2026-03-10`) `1,750,000원`, 미수
- 계산:
  - `0 + 1,750,000 = 1,750,000`
- 현재 고객 `outstanding_balance`: `1,750,000`

## 코드 관점 리스크

- 현재 고객 재계산 로직은 [api.ts](/Users/jangjiho/workspace/pressco21/offline-crm-v2/src/lib/api.ts#L567)에서 `CRM 명세표 미수 합계`만 다시 계산한다.
- 구현상 최종 patch는 `outstanding_balance: Math.max(0, outstanding)`이며, 레거시 baseline을 합산하지 않는다.
- 즉, 지금 데이터가 맞게 보이더라도 `recalcCustomerStats()`가 다시 실행되면 아래처럼 왜곡될 수 있다.

| 고객 | 현재 맞는 값 | 재계산 시 예상값 | 왜곡 |
|------|-------------|------------------|------|
| 신재승 회장님 | -581,000 | 626,900 | 선수금/마이너스 잔액 소실 |
| 채명례 이사장님 | -703,930 | 70,200 | 선수금 소실 |
| 서은희 회장님 | 37,200 | 200,200 | 레거시 마이너스 baseline 소실 |

## 판정

- `6건 불일치`는 더 이상 복구 미완료 이슈가 아니다.
- 실제 미완료 항목은 `레거시 잔액 baseline을 안전하게 보존하는 재계산 정책`이다.

## 권장 다음 단계

1. 고객 테이블에 `legacy_balance_baseline` 같은 별도 필드를 두고, 원본값을 고정 저장한다.
2. 고객 잔액 재계산은 `baseline + CRM 미수 합계`로 바꾼다.
3. 음수 잔액을 0으로 clamp하는 현재 로직은 제거하거나, 표시용 값과 회계 원장을 분리한다.
4. 고객 상세/미수금 화면에서 `레거시 baseline`, `CRM 미수`, `현재 운영 잔액`을 분리 표기한다.
